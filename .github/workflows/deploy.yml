# This workflow has been superseded. See:
#   pr-checks.yml       — PR security/quality gates
#   deploy-app.yml      — application deployment on push to main
#   terraform-apply.yml — infrastructure apply on push to main
#
# Kept here as documentation only. No triggers are active.

name: "(Deprecated — see deploy-app / terraform-apply / pr-checks)"
on:
  workflow_dispatch:
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Replaced by: deploy-app.yml, terraform-apply.yml, pr-checks.yml"
