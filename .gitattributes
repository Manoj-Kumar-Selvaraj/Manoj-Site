# Enforce LF line endings in the repository (prevents CRLF from breaking CI tools)
* text=auto eol=lf

# Explicitly LF for files parsed by Linux CI tools
.trivyignore text eol=lf
.env* text eol=lf
*.sh text eol=lf
